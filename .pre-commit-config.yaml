repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    # Ensure this matches or exceeds your pyproject.toml version
    rev: v0.15.5 
    hooks:
      # Run the linter (replaces 'ruff-check')
      - id: ruff
        args: [ --fix ]
      # Run the formatter
      - id: ruff-format